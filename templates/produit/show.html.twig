{% extends 'base-front.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ produit.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ produit.prix }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ produit.photo }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ produit.description }}</td>
            </tr>
            <tr>
                <th>Disponibilte</th>
                <td>{{ produit.disponibilte }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('produit_index') }}">Consulter la liste de materiels</a>
{% endblock %}
{#    <a href="{{ path('produit_edit', {'id': produit.id}) }}">edit</a>#}

{#    {{ include('produit/_delete_form.html.twig') }}#}

