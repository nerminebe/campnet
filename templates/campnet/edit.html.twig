{% extends 'campnet.html.twig' %}


{% block title %}Edit Campnet{% endblock %}

{% block body %}
    <h1>Edit Campnet</h1>

    {{ include('campnet/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('campnet_index') }}">back to list</a>

    {{ include('campnet/_delete_form.html.twig') }}
{% endblock %}
